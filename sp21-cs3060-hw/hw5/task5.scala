object task5 {
  def main(arg: Array[Strings]) = {
    myStringSpilt(str, flag)
  }

  def myStringSpilt(str: String, flag: Char) = {
    var flagLoc = Array()
    for (i <- 1 until str.length()) {
      if (str(i) == flag) {}
    }

  }
}
